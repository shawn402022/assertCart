from old_cart import cStoreCart,cStore,shopppingCart,shippingCost,countItems,total,addItem

import unittest

class AddressBookTest(unitest.TestCase):
    def test_cStoreCart(self):
        self.assertIsNot(shoppingCart,shoppingCost)

        self.assertIs (type(dictionary),cStore)
        

        self.assertIn(frozen pizza,shoppingCart)

        self.assertRaises(Exception,cStoreCart)

        